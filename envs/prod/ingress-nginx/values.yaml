ingress-nginx:
  controller:
    service:
      type: LoadBalancer

    resources:
      requests:
        cpu: 100m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 512Mi

    nodeSelector:
      kubernetes.io/os: linux

    admissionWebhooks:
      enabled: false 
